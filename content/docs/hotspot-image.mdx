---
title: HotspotImage
description: Shoppable scenes with positioned pins, popovers, and embeddable product content.
---

import {
  HotspotBasicSceneExample,
  HotspotContentTypesExample,
  HotspotMultipleScenesExample,
  HotspotOverlayInPinExample,
  HotspotPinVariantsExample,
  HotspotResponsiveExample,
} from "@/components/docs/hotspot-image-examples"

export const hotspotImageProps = {
  src: {
    type: <code>string</code>,
    required: true,
    description: "Image source for the scene.",
  },
  alt: {
    type: <code>string</code>,
    required: true,
    description: "Accessible alt text for the scene image.",
  },
  width: {
    type: <code>number</code>,
    required: true,
    description: "Natural image width in pixels.",
  },
  height: {
    type: <code>number</code>,
    required: true,
    description: "Natural image height in pixels.",
  },
  children: {
    type: <code>React.ReactNode</code>,
    required: true,
    description: "HotspotPin children rendered over the image.",
  },
  className: {
    type: <code>string</code>,
    description: "Optional className for the outer scroll region.",
  },
}

export const hotspotPinProps = {
  x: {
    type: <code>number</code>,
    required: true,
    description: "Horizontal pin position as a percentage of image width.",
  },
  y: {
    type: <code>number</code>,
    required: true,
    description: "Vertical pin position as a percentage of image height.",
  },
  variant: {
    type: <code>"plus" | "dot"</code>,
    default: <code>"plus"</code>,
    description: "Visual pin style.",
  },
  label: {
    type: <code>string</code>,
    description: "Accessible label for the trigger button.",
  },
  children: {
    type: <code>React.ReactNode</code>,
    required: true,
    description: "Popover content shown when the pin opens.",
  },
  className: {
    type: <code>string</code>,
    description: "Optional className for the pin trigger.",
  },
}

export const hotspotTooltipProps = {
  title: {
    type: <code>string</code>,
    required: true,
    description: "Tooltip heading.",
  },
  description: {
    type: <code>string</code>,
    required: true,
    description: "Tooltip body copy.",
  },
  className: {
    type: <code>string</code>,
    description: "Optional className for the tooltip container.",
  },
}

export const hotspotLinkProps = {
  label: {
    type: <code>string</code>,
    required: true,
    description: "Visible link label.",
  },
  href: {
    type: <code>string</code>,
    required: true,
    description: "Link destination.",
  },
  target: {
    type: <code>React.HTMLAttributeAnchorTarget</code>,
    description: "Optional anchor target such as `_blank`.",
  },
  className: {
    type: <code>string</code>,
    description: "Optional className for the link container.",
  },
}

# HotspotImage

HotspotImage turns editorial scenes and product photography into interactive
entry points. It keeps the source image at its natural dimensions, places pins
using percentage coordinates, and lets each pin open any content you need.

## Installation

```bash
npx shadcn@latest add @shoppablecn/hotspot-image
```

## Examples

### Basic scene

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotBasicSceneExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { HotspotTooltip } from "@/components/ui/hotspot-tooltip"
import { mockSceneLivingRoom } from "@/lib/mock-data"

export function Example() {
  return (
    <HotspotImage
      alt={mockSceneLivingRoom.alt}
      height={mockSceneLivingRoom.height}
      src={mockSceneLivingRoom.src}
      width={mockSceneLivingRoom.width}
    >
      <HotspotPin x={24} y={52}>
        <HotspotTooltip
          description="Solid oak table styling the scene with warm tonal contrast."
          title="Oak side table"
        />
      </HotspotPin>
    </HotspotImage>
  )
}
```

  </Tab>
</Tabs>

### Pin variants

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotPinVariantsExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { HotspotTooltip } from "@/components/ui/hotspot-tooltip"
import { mockSceneProductHero } from "@/lib/mock-data"

export function Example() {
  return (
    <HotspotImage
      alt={mockSceneProductHero.alt}
      height={mockSceneProductHero.height}
      src={mockSceneProductHero.src}
      width={mockSceneProductHero.width}
    >
      <HotspotPin variant="plus" x={36} y={28}>
        <HotspotTooltip description="Default plus pin." title="Plus pin" />
      </HotspotPin>
      <HotspotPin variant="dot" x={74} y={74}>
        <HotspotTooltip description="Compact dot pin." title="Dot pin" />
      </HotspotPin>
    </HotspotImage>
  )
}
```

  </Tab>
</Tabs>

### Content types in pins

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotContentTypesExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotLink } from "@/components/ui/hotspot-link"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { HotspotTooltip } from "@/components/ui/hotspot-tooltip"
import { ProductCard } from "@/components/ui/product-card"
import { mockProductSimple, mockSceneMixedContent } from "@/lib/mock-data"

export function Example() {
  return (
    <HotspotImage
      alt={mockSceneMixedContent.alt}
      height={mockSceneMixedContent.height}
      src={mockSceneMixedContent.src}
      width={mockSceneMixedContent.width}
    >
      <HotspotPin x={52} y={32}>
        <HotspotTooltip
          description="Soft-loop lining keeps warmth high without adding bulk."
          title="Brushed fleece interior"
        />
      </HotspotPin>
      <HotspotPin variant="dot" x={79} y={20}>
        <HotspotLink href="/docs/product-card" label="Open the lookbook" />
      </HotspotPin>
      <HotspotPin x={22} y={64}>
        <div className="w-[320px]">
          <ProductCard product={mockProductSimple} variants="none" />
        </div>
      </HotspotPin>
    </HotspotImage>
  )
}
```

  </Tab>
</Tabs>

### ProductCard overlay inside a pin

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotOverlayInPinExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { ProductCard } from "@/components/ui/product-card"
import { mockProductWithVariants, mockSceneMixedContent } from "@/lib/mock-data"

export function Example() {
  return (
    <HotspotImage
      alt={mockSceneMixedContent.alt}
      height={mockSceneMixedContent.height}
      src={mockSceneMixedContent.src}
      width={mockSceneMixedContent.width}
    >
      <HotspotPin x={22} y={64}>
        <div className="w-[320px]">
          <ProductCard
            onAddToCart={() => {}}
            product={mockProductWithVariants}
            variants="overlay"
          />
        </div>
      </HotspotPin>
    </HotspotImage>
  )
}
```

  </Tab>
</Tabs>

### Mobile-width responsiveness

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotResponsiveExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { HotspotTooltip } from "@/components/ui/hotspot-tooltip"
import { mockSceneLivingRoom } from "@/lib/mock-data"

export function Example() {
  return (
    <div className="w-[375px] max-w-full">
      <HotspotImage
        alt={mockSceneLivingRoom.alt}
        height={mockSceneLivingRoom.height}
        src={mockSceneLivingRoom.src}
        width={mockSceneLivingRoom.width}
      >
        <HotspotPin variant="dot" x={77} y={28}>
          <HotspotTooltip
            description="Narrow containers stay horizontally scrollable."
            title="Horizontal overflow"
          />
        </HotspotPin>
      </HotspotImage>
    </div>
  )
}
```

  </Tab>
</Tabs>

### Multiple scenes side by side

<Tabs items={["Preview", "Code"]}>
  <Tab>
    <HotspotMultipleScenesExample />
  </Tab>
  <Tab>

```tsx
import { HotspotImage } from "@/components/ui/hotspot-image"
import { HotspotPin } from "@/components/ui/hotspot-pin"
import { mockSceneLivingRoom, mockSceneProductHero } from "@/lib/mock-data"

export function Example() {
  return (
    <div className="grid gap-4 xl:grid-cols-2">
      <HotspotImage
        alt={mockSceneLivingRoom.alt}
        height={mockSceneLivingRoom.height}
        src={mockSceneLivingRoom.src}
        width={mockSceneLivingRoom.width}
      >
        <HotspotPin x={24} y={52}>...</HotspotPin>
      </HotspotImage>
      <HotspotImage
        alt={mockSceneProductHero.alt}
        height={mockSceneProductHero.height}
        src={mockSceneProductHero.src}
        width={mockSceneProductHero.width}
      >
        <HotspotPin x={55} y={54}>...</HotspotPin>
      </HotspotImage>
    </div>
  )
}
```

  </Tab>
</Tabs>

## Props

### HotspotImage

<TypeTable type={hotspotImageProps} />

### HotspotPin

<TypeTable type={hotspotPinProps} />

### HotspotTooltip

<TypeTable type={hotspotTooltipProps} />

### HotspotLink

<TypeTable type={hotspotLinkProps} />

## Accessibility

- Pins are real interactive buttons with labels.
- Popovers close on Escape and return focus to the trigger.
- Scene regions remain horizontally scrollable instead of collapsing the image.
- Embedded ProductCard content keeps its own keyboard behavior inside the popover.

## Related

- [ProductCard](/docs/product-card) can be rendered directly inside hotspot pins.
